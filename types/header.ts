export interface HandleSearchBarProps {
  searchBar: boolean;
  setSearchBar: (searchBar: boolean) => void;
}

export interface MobileNavbarProps {
  isOpen: boolean;
  setIsOpen: (isOpen: boolean) => void;
}
